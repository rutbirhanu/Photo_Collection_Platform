// src/app/album/[albumId]/page.tsx
export default function AlbumPage({ params }: { params: { albumId: string } }) {
  // fetch data or redirect
//   return <Redirect to={`/photo/${params.albumId}`} />;
}